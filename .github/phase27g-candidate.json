{"base":"b7d89dac7343d8a5c7f446f1ad0255c93d6ea058","files":{"src/rendering/exposure-meter.ts":"f273d51d8ec25da095feedb330b47a2c70c56011","src/rendering/adaptive-exposure.ts":"6a792443152d060803fc04080cee9e7049f9d739","src/rendering/frame-capture.ts":"f94506d77a91e28406ffb02ece8abd16e8417a44","src/rendering/local-atmosphere.ts":"e705f3980d71e147e9ca2bb5f84e7096d95b387d","src/rendering/race-composition.ts":"403b2b1ad75a583269c2b1057056273fd1e5812e","src/rendering/driver-tailoring.ts":"6a2829044ee0118125697d8f0a5e1fc556cf8f43","src/ui/tab-evidence.ts":"a0adcded13faa341dbe646d210dc27ec02417c65","tests/phase27g-completion.test.ts":"3840c8b8738245941ca679612b371b57623c4265","tests/tab-evidence.test.ts":"b9d625d145b55d19b570454ec48d549dd620482e","e2e/fixtures/phase27g-completion.ts":"8d374e9c9328afdf6e63ed612c3e60d8fc93b17b","e2e/19-phase27g-completion.spec.ts":"941c199475481c3be0504f91adf829241873d9d3","docs/PHASE_27G_CLOSURE_EVIDENCE.md":"33641322bc54eb0510a15d2f94cfdea8e3eb4e1b"},"edits":[{"path":"src/rendering/renderer.ts","old":"import { reviewHardware }","new":"import { AdaptiveExposurePass } from './adaptive-exposure.ts';\nimport { LocalAtmosphere } from './local-atmosphere.ts';\nimport { RaceComposition } from './race-composition.ts';\nimport { captureRenderedCanvas } from './frame-capture.ts';\nimport { reviewHardware }","count":1},{"path":"src/rendering/renderer.ts","old":"import { TracksideDirector, broadcastRadius }","new":"import { TracksideDirector }","count":1},{"path":"src/rendering/renderer.ts","old":"  private composer: EffectComposer;","new":"  private composer: EffectComposer;\n  private exposure = new AdaptiveExposurePass();\n  private atmosphere: LocalAtmosphere;\n  private composition = new RaceComposition();","count":1},{"path":"src/rendering/renderer.ts","old":"    this.gpuTimer = new GpuTimer(context);","new":"    this.gpuTimer = new GpuTimer(context);\n    this.atmosphere = new LocalAtmosphere(track);","count":1},{"path":"src/rendering/renderer.ts","old":"    this.trackside = new TracksideDirector(track, (from, to) =>","new":"    this.circuit.construction.add('Local weather materials', 1, () => this.atmosphere.install(this.scene));\n    this.trackside = new TracksideDirector(track, (from, to) =>","count":1},{"path":"src/rendering/renderer.ts","old":"    this.composer.addPass(new RenderPass(this.scene, this.camera));","new":"    this.composer.addPass(new RenderPass(this.scene, this.camera));\n    this.composer.addPass(this.exposure);","count":1},{"path":"src/rendering/renderer.ts","old":"      this.textures.register(car.root);","new":"      this.textures.register(car.root);\n      this.atmosphere.install(car.root);","count":1},{"path":"src/rendering/renderer.ts","old":"  resize() {\n    const w","new":"  resize() {\n    this.exposure.reset();\n    const w","count":1},{"path":"src/rendering/renderer.ts","old":"  capturePhoto(): Promise<Blob> {\n    return new Promise((resolve, reject) => {\n      this.canvas.toBlob(\n        (blob) => (blob ? resolve(blob) : reject(new Error('PNG capture failed'))),\n        'image/png',\n      );\n    });\n  }","new":"  capturePhoto(): Promise<Blob> {\n    // Called in the same presentation task: own the pixels before async PNG encoding.\n    return captureRenderedCanvas(this.canvas);\n  }","count":1},{"path":"src/rendering/renderer.ts","old":"  changeCamera(mode?: CameraMode) {\n    this.motionBlur.reset();","new":"  changeCamera(mode?: CameraMode) {\n    this.exposure.reset();\n    this.composition.reset();\n    this.motionBlur.reset();","count":1},{"path":"src/rendering/renderer.ts","old":"  reset() {\n    this.reflection.invalidate();","new":"  reset() {\n    this.exposure.reset();\n    this.composition.reset();\n    this.reflection.invalidate();","count":1},{"path":"src/rendering/renderer.ts","old":"    if (this.cameraClock.discontinuous) {\n      this.inertia.reset();","new":"    if (this.cameraClock.discontinuous) {\n      this.exposure.reset();\n      this.composition.reset();\n      this.inertia.reset();","count":1},{"path":"src/rendering/renderer.ts","old":"    } else if (this.mode === 'trackside') {\n      this.trackside.update(","new":"    } else if (this.mode === 'trackside') {\n      const composition = this.composition.update(presented, this.follow);\n      this.trackside.update(","count":1},{"path":"src/rendering/renderer.ts","old":"        this.target,\n        this.temporary.set(presented[o + F.VX], presented[o + F.VY], presented[o + F.VZ]),\n        cameraDt,\n        this.camera.aspect,\n        broadcastRadius(presented, this.follow),","new":"        composition.target,\n        composition.velocity,\n        cameraDt,\n        this.camera.aspect,\n        composition.radius,\n        true,","count":1},{"path":"src/rendering/renderer.ts","old":"    this.circuit.update(b);","new":"    this.circuit.update(b);\n    this.atmosphere.update(presented, this.graphics.localFog && !studio);","count":1},{"path":"src/rendering/renderer.ts","old":"      this.composer.render();","new":"      // The meter sees linear scene pixels; only the final output exposure adapts.\n      // Photo exposure and studio lighting remain manual and exactly held.\n      this.renderer.toneMappingExposure = this.exposure.prepare(\n        presented[H.TIME], `${illumination}:${this.mode}:${this.mode === 'trackside' ? this.trackside.activeId : -1}:${this.follow}`,\n        daylight.exposure * 2 ** (this.photo?.exposure ?? 0),\n        this.graphics.autoExposure && !this.photo && !menu,\n      );\n      this.composer.render();","count":1},{"path":"src/rendering/renderer.ts","old":"      motionBlur: this.motionBlur.diagnostics(),","new":"      motionBlur: this.motionBlur.diagnostics(),\n      automaticExposure: this.exposure.diagnostics(),\n      localAtmosphere: this.atmosphere.diagnostics(),\n      raceComposition: this.composition.diagnostics(),","count":1},{"path":"src/rendering/renderer.ts","old":"    this.motionBlur.dispose();","new":"    this.motionBlur.dispose();\n    this.exposure.dispose();","count":1},{"path":"src/rendering/options.ts","old":"  bloom: boolean;","new":"  bloom: boolean;\n  autoExposure: boolean;\n  localFog: boolean;","count":1},{"path":"src/rendering/options.ts","old":"    bloom: quality === 'high',","new":"    bloom: quality === 'high',\n    autoExposure: quality !== 'low',\n    localFog: quality !== 'low',","count":1},{"path":"src/rendering/options.ts","old":"    bloom: typeof p.bloom === 'boolean' ? p.bloom : fallback.bloom,","new":"    bloom: typeof p.bloom === 'boolean' ? p.bloom : fallback.bloom,\n    autoExposure: typeof p.autoExposure === 'boolean' ? p.autoExposure : fallback.autoExposure,\n    localFog: typeof p.localFog === 'boolean' ? p.localFog : fallback.localFog,","count":1},{"path":"src/ui/presentation.ts","old":"  ['bloom', 'Bloom post-processing'],","new":"  ['bloom', 'Bloom post-processing'],\n  ['autoExposure', 'Adaptive exposure (measured scene luminance)'],\n  ['localFog', 'Weather-driven low-lying haze'],","count":1},{"path":"src/rendering/trackside.ts","old":"  update(s: number, target: Vector3, velocity: Vector3, dt: number, aspect = 16 / 9, radius = 3.1) {","new":"  update(s: number, target: Vector3, velocity: Vector3, dt: number, aspect = 16 / 9, radius = 3.1, strictRadius = false) {","count":1},{"path":"src/rendering/trackside.ts","old":"    if (!framing.fits && radius > 3.1) {","new":"    if (!framing.fits && radius > 3.1 && !strictRadius) {","count":1},{"path":"src/rendering/trackside.ts","old":"    const cut = id !== this.activeId || seek;","new":"    if (strictRadius && radius > 3.1 && !tracksideFraming(Math.max(0.001, this.rigs[id].position.distanceTo(target)), this.rigs[id].baseFov, aspect, radius).fits) {\n      // A real pack requires a suitable physical camera, not a secretly reduced\n      // bounding sphere. Prefer the nearest unobstructed authored rig that fits.\n      const candidates = this.rigs.map(r => r.id).filter(candidate => candidate !== id);\n      candidates.sort((a,b) => distance(s,this.rigs[a].centerS)-distance(s,this.rigs[b].centerS) || a-b);\n      const replacement = candidates.find(candidate => {\n        const rig = this.rigs[candidate];\n        return !(this.blocked?.(rig.position,target) ?? false) &&\n          tracksideFraming(Math.max(0.001,rig.position.distanceTo(target)),rig.baseFov,aspect,radius).fits;\n      });\n      if (replacement !== undefined) { id = replacement; this.occluded = false; }\n    }\n    const cut = id !== this.activeId || seek;","count":1},{"path":"src/rendering/driver-anatomy.ts","old":"import { fingerGripCurve }","new":"import { tailoredSleeve } from './driver-tailoring.ts';\nimport { fingerGripCurve }","count":1},{"path":"src/rendering/driver-anatomy.ts","old":"export function sleeveGeometry(upper: boolean) {\n  const bottom = upper ? 0.052 : 0.041,\n    top = upper ? 0.044 : 0.029;\n  const points = [new T.Vector2(0, -0.5)];\n  for (let i = 0; i <= 20; i++) {\n    const u = i / 20;\n    const fold = 1 + 0.04 * Math.sin(u * Math.PI * 8) * Math.sin(u * Math.PI) ** 2;\n    points.push(new T.Vector2((bottom + (top - bottom) * u) * fold, u - 0.5));\n  }\n  points.push(new T.Vector2(0, 0.5));\n  const geometry = new T.LatheGeometry(points, 20);\n  // The flattened sleeve cross-section has volume, rather than looking like a\n  // pipe at the wrist. Bone length/IK are still owned by DriverRig.\n  geometry.scale(1.06, 1, 0.91);\n  const p = geometry.getAttribute('position');\n  for (let i = 0; i < p.count; i++) {\n    const y = p.getY(i),\n      x = p.getX(i),\n      z = p.getZ(i);\n    const angle = Math.atan2(z, x),\n      joint = Math.exp(-(((y + 0.18) / 0.17) ** 2));\n    const crease =\n      1 + joint * (0.035 * Math.sin(y * 39 + angle * 2) + 0.018 * Math.sin(y * 67 - angle));\n    p.setXYZ(i, x * crease, y, z * crease);\n  }\n  geometry.computeVertexNormals();\n  return geometry;\n}","new":"export function sleeveGeometry(upper: boolean) {\n  return tailoredSleeve(upper);\n}","count":1},{"path":"src/rendering/car-architecture.ts","old":"import { bodySurface, ENGINE_SECTIONS }","new":"import { capSafetyTube } from './driver-tailoring.ts';\nimport { bodySurface, ENGINE_SECTIONS }","count":1},{"path":"src/rendering/car-architecture.ts","old":"  return new T.TubeGeometry(\n    curve,","new":"  return capSafetyTube(new T.TubeGeometry(\n    curve,","count":1},{"path":"src/rendering/car-architecture.ts","old":"    detail === 'high' ? 12 : 8,\n    false,\n  );","new":"    detail === 'high' ? 12 : 8,\n    false,\n  ));","count":1},{"path":"src/main.ts","old":"import { ReferenceSessionReview, referenceSessionPanel }","new":"import { TabEvidence } from './ui/tab-evidence.ts';\nimport { ReferenceSessionReview, referenceSessionPanel }","count":1},{"path":"src/main.ts","old":"  private sessionReview = new SessionReview();","new":"  private sessionReview = new SessionReview();\n  private tabEvidence = new TabEvidence(__APEX_SOURCE_FINGERPRINT__);","count":1},{"path":"src/main.ts","old":"    this.sessionReview.event(performance.now(), 'action', name);","new":"    this.sessionReview.event(performance.now(), 'action', name);\n    this.tabEvidence.observe('action', name);","count":1},{"path":"src/main.ts","old":"          !!this.sessionReview.report(),\n        );\n        break;","new":"          !!this.sessionReview.report(),\n        );\n        this.tabEvidence.mount(this.ui.get('modalContent'));\n        break;","count":1},{"path":"src/main.ts","old":"    this.sessionReview.stop('Application disposed', true);","new":"    this.sessionReview.stop('Application disposed', true);\n    this.tabEvidence.dispose();","count":1},{"path":"src/main.ts","old":"    this.sessionReview.stop('Application error', true);","new":"    this.sessionReview.stop('Application error', true);\n    this.tabEvidence.observe('application-error', error instanceof Error ? error.message : String(error));","count":1},{"path":"src/main.ts","old":"      referenceEvent: this.referenceSession.watch.report(),","new":"      tabEvidence: this.tabEvidence.report(),\n      referenceEvent: this.referenceSession.watch.report(),","count":1},{"path":"src/ui/interface.ts","old":"This is not every physics tick or an audiovisual recording: retain an external uninterrupted recording from application launch and use the separate lap recorder for targeted game-audio clips.","new":"These snapshots are not every physics tick or an audiovisual recording. The separate direct-to-file tab recorder includes the actual HTML HUD and optional tab audio from when you start it. Retain an external uninterrupted recording for application launch.","count":1},{"path":"README.md","old":"## Phase 27G development candidate","new":"## Phase 27G development candidate\n\nThe closure continuation adds measured adaptive exposure, local weather haze, complete-group broadcast framing, revised sleeve/safety-cell geometry, same-task PNG ownership and explicit full-tab recording to disk. See [closure implementation and remaining independent acceptance](docs/PHASE_27G_CLOSURE_EVIDENCE.md).","count":1}]}
