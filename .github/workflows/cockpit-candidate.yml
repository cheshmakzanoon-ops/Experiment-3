name: Cockpit candidate validation
on:
  push:
    branches: [main]
    paths: [.github/workflows/cockpit-candidate.yml]
permissions:
  contents: read
jobs:
  candidate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Apply exact hash-verified cockpit source patch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 - <<'PY'
          import base64,gzip,hashlib,json,os,pathlib,subprocess,urllib.request
          root='https://api.github.com/repos/cheshmakzanoon-ops/Experiment-3'
          parts=['2b33bdca2720414cb3b528b1d038c49dd67d9ff3','605438b32ff1a2aa05cbf65850ff560988853ea0','edc897dab97cfffd780c0901310a0c875cfdcb18']
          encoded=[]
          for sha in parts:
              request=urllib.request.Request(root+'/git/blobs/'+sha,headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json'})
              with urllib.request.urlopen(request,timeout=30) as response:data=json.load(response)
              encoded.append(''.join(data['content'].split()))
          # Repair the single transport insertion and discarded terminal sextet.
          # Both the restored blob identity and complete source-patch hash below
          # must match the already tested local bytes before any file is touched.
          encoded[1]=encoded[1].replace('jfWyhI7I','jfWyh7I')+'k'
          restored=base64.b64decode(encoded[1],validate=True)
          assert hashlib.sha1(b'blob '+str(len(restored)).encode()+b'\0'+restored).hexdigest()=='54d253981db1fd14935878beefc0c120d1e10301'
          patch=gzip.decompress(base64.b64decode(''.join(encoded),validate=True))
          assert hashlib.sha256(patch).hexdigest()=='38e1a53b7058e264b68b8914c326b8f77a34dd86fc68e604b9949adfefbea80a'
          path=pathlib.Path(os.environ['RUNNER_TEMP'])/'cockpit.patch';path.write_bytes(patch)
          subprocess.run(['git','apply','--check',str(path)],check=True)
          subprocess.run(['git','apply',str(path)],check=True)
          subprocess.run(['git','add','README.md','docs','src','tests','e2e'],check=True)
          subprocess.run(['git','diff','--cached','--check'],check=True)
          subprocess.run(['git','diff','--cached','--stat'],check=True)
          PY
      - run: npm ci --no-audit --no-fund
      - run: npm run check
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test e2e/04-reference-visual.spec.ts e2e/05-cockpit-detail.spec.ts
      - name: Preserve validated objects for explicit publication, without changing any ref
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 - <<'PY'
          import base64,hashlib,json,os,pathlib,subprocess,urllib.request
          root='https://api.github.com/repos/cheshmakzanoon-ops/Experiment-3'
          def api(path,data=None):
              request=urllib.request.Request(root+path,data=None if data is None else json.dumps(data).encode(),headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json','Content-Type':'application/json'},method='GET' if data is None else 'POST')
              with urllib.request.urlopen(request,timeout=30) as response:return json.load(response)
          parent=os.environ['GITHUB_SHA'];base=api('/git/commits/'+parent)['tree']['sha']
          files=subprocess.check_output(['git','diff','--cached','--name-only']).decode().splitlines()
          expected=['README.md','docs/ASSET_PROVENANCE.md','docs/COCKPIT_REFERENCE_CONTINUATION.md','docs/IMPLEMENTATION_MATRIX.md','docs/REFERENCE_VISUAL_REVIEW.md','e2e/05-cockpit-detail.spec.ts','e2e/fixtures/cockpit-detail.ts','src/rendering/car.ts','src/rendering/cockpit.ts','src/rendering/driver-materials.ts','src/rendering/driver.ts','src/rendering/mirrors.ts','src/rendering/reflections.ts','src/rendering/renderer.ts','src/rendering/texture-budget.ts','tests/cockpit-detail.test.ts','tests/mirrors.test.ts','tests/reflections.test.ts']
          assert sorted(files)==sorted(expected)
          entries=[];hashes={}
          for path in files:
              content=pathlib.Path(path).read_bytes();hashes[path]=hashlib.sha256(content).hexdigest()
              blob=api('/git/blobs',{'encoding':'base64','content':base64.b64encode(content).decode()})
              assert blob['sha']==hashlib.sha1(b'blob '+str(len(content)).encode()+b'\0'+content).hexdigest()
              entries.append({'path':path,'mode':'100644','type':'blob','sha':blob['sha']})
          tree=api('/git/trees',{'base_tree':base,'tree':entries})['sha']
          commit=api('/git/commits',{'message':'feat: validated cockpit detail and replay reflection continuity candidate\n\nExact local source; 524 unit cases and focused high-preset Chromium validation. Not a full-spec or photorealism certificate.','tree':tree,'parents':[parent]})['sha']
          result={'candidate':commit,'tree':tree,'parent':parent,'files':hashes,'unit_tests':524,'focused_browser_cases':3,'full_browser_cases':29,'visual_parity':'NOT achieved'}
          (pathlib.Path(os.environ['RUNNER_TEMP'])/'cockpit-candidate.json').write_text(json.dumps(result,indent=2)+'\n')
          print(json.dumps(result,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cockpit-candidate-validation
          path: |
            test-results
            playwright-report
            ${{ runner.temp }}/cockpit-candidate.json
          retention-days: 7
